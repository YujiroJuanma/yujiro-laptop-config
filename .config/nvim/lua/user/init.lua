return {
  options = {
    opt = {
      shiftwidth = 8, -- Size of an indentation
      tabstop = 8,    -- Size of a hard tabstop
      softtabstop = 8, -- Number of spaces a <Tab> counts for in insert mode
      expandtab = false, -- Use spaces instead of tab characters
    },
    -- You can also add global options (g) or window options (w) here
  },
  -- other configurations like plugins, etc.
}

